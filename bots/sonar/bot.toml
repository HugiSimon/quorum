name = "sonar"
role = "research"
hue = 250

command = "gemini"
args = ["--acp"]
model = ""

[env]
NODE_EXTRA_CA_CERTS = "${NODE_EXTRA_CA_CERTS}"
HTTPS_PROXY = "${HTTPS_PROXY}"
NO_PROXY = "${NO_PROXY}"
